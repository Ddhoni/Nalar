#[test]
fn dummy_health_test() {
    // This is a placeholder to ensure the tests/ folder exists in the template.
    // Real integration tests can be added later (spawning the axum app).
    assert_eq!(2 + 2, 4);
}
